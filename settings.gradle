pluginManagement {
    repositories {
        maven { url = 'https://repo.spring.io/release' }
        gradlePluginPortal()
        mavenCentral()
    }
}
dependencyResolutionManagement { repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS); repositories { mavenCentral() } }
rootProject.name = 'panpukha_java_final_hw'
